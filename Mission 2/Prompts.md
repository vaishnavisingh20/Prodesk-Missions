## Prompt 1
```bash
addBtn.addEventListener('click', addExpense);
setSalaryBtn.addEventListener('click', setSalary);
exportBtn.addEventListener('click', exportPDF);
```
The buttons in the code are not working
